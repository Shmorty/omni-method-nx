import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shmorty.omni-method',
  appName: 'omni-method',
  webDir: '../../dist/apps/omni-method',
  bundledWebRuntime: false,
};

export default config;
